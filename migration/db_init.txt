CREATE TABLE IF NOT EXISTS user(id INTEGER PRIMARY KEY, login TEXT DEFAULT(""), password TEXT DEFAULT(""), name TEXT DEFAULT(""), age TEXT DEFAULT(""));

CREATE TABLE IF NOT EXISTS phone(id INTEGER PRIMARY KEY, phone TEXT, description TEXT, is_fax TINYINT, user_id INTEGER, FOREIGN KEY(user_id) REFERENCES user(id));